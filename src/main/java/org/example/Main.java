package org.example;

/**
 * Business logic: placeholder entry point for this collection of standalone
 * concurrency examples.
 *
 * <p>Technique: keeps the Maven project executable without coupling the examples
 * to one shared application flow. This lets each package remain self-contained
 * while still providing a standard entry point.
 */
public class Main {

    private Main() {
    }

    /**
     * No-op application entry point.
     *
     * @param args command-line arguments, currently unused
     */
    public static void main(String[] args) {
    }
}
